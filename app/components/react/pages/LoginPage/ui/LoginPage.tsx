import { LoginForm } from '@/features/Login';

const LoginPage = () => {
    return (
        <div
            data-testid="HomePage"
        >
            <LoginForm />
        </div>
    );
};

export default LoginPage;
